parameters:
  tfDir: ""
  tfCliShow: ""

steps:
  - bash: |
      terraform show
    displayName: "Terraform Plan"
    name: showplan
    workingDirectory: ${{ parameters.tfDir}}
    enabled: "true"
    env:
      TF_CLI_ARGS_show: ${{ parameters.tfCliShow }}
