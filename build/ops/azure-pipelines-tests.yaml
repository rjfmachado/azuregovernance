name: Test runs$(Rev:.r)

trigger: none

pr: none

variables:
  - group: azuregovernancedev

stages:
  - stage: dev

    variables:
      - name: deploymentStage
        value: "dev"

    jobs:
      - job: Test
        displayName: Tests
        pool:
          vmImage: "ubuntu-latest"

        steps:
          - task: AzurePowerShell@4
            inputs:
              azureSubscription: scAzureGovernance
              scriptType: Inline
              inline: |
                Install-Module -Name Pester -Force -Scope CurrentUser
              azurePowerShellVersion: "latestVersion"

          - task: AzurePowerShell@4
            inputs:
              azureSubscription: scAzureGovernance
              scriptType: Inline
              inline: |
                Get-AzSubscription
              azurePowerShellVersion: "latestVersion"
